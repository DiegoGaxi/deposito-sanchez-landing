# Depósito Sánchez — sitio público

Landing de marketing + demo del POS (build estático). El **código fuente** vive
en el repositorio privado; aquí solo se publica el sitio ya compilado.

- `/` — landing
- `/app/` — demo del POS (modo demo, sin backend)
- Descarga del instalador: se publica como asset en **Releases** de este repo.

Deploy automático a GitHub Pages vía `.github/workflows/pages.yml` en cada push a `main`.
